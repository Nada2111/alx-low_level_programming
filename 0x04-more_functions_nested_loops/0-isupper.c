#include "main.h"
 /**
     *  _isupper - function that checks for uppercase character
     *  int @i
*/
 int _isupper(int i)
{
    if (i >= 65 && i <= 90)
    {
    return (1);
    }
    return (0);
}
